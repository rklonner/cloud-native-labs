apiVersion: v1
kind: Service
metadata:
  name: argo-workflows-server-local
  namespace: argo
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  type: NodePort
  selector:
    app.kubernetes.io/instance: lab-clone-build-push-sign-harbor-argo-workflows
    app.kubernetes.io/name: argo-workflows-server
  ports:
    - name: http
      port: 2746
      targetPort: 2746
      nodePort: 32746
